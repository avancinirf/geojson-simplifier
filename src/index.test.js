import GeojsonSimplifier from '.'

describe('GeojsonSimplifier', () => {
  let geojsonSimplifier

  beforeEach(() => {
    geojsonSimplifier = GeojsonSimplifier
  })

  it('should ...', () => {
    expect(geojsonSimplifier.getTeste()).toBe('Minha funcao esta ok!!!')
  })
})
